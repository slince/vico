// @vico/core — 语义记忆默认模板（各 SemanticMemory 实现共享）
/** 语义记忆默认 Markdown 模板 — 各实现未显式传入 template 时的兜底 */
export const DEFAULT_SEMANTIC_MEMORY_TEMPLATE = `# 用户信息
- **姓名**：
- **位置**：
- **时区**：
- **语言**：

## 偏好设置
- **沟通风格**：

## 会话上下文
- **当前任务**：
`;
